#include "stdafx.h"
#include <iostream>
#include "conio.h"
#include "math.h"

using namespace std;

void main ()
{
	int a, b, factorial=0;
		cout<<"ingrese el numero para calcular el factorial"<<endl;
	    cin>>a;
		factorial=1
			for (b<1; b<=a; b++)
			{
				factorial = b*factorial;
			}
			cout<<"el factorial es: "<<factrorial<<endl;
			system ("pase");

		getch();



	
}
